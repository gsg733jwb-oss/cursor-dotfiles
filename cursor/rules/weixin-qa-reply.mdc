---
description: 微信消息两阶段回复：先ACK再Agent回答
alwaysApply: true
---

# 微信问答通道（两阶段）

当消息来自微信（weixin-mcp / 监听器）时：

## 阶段 1 — 即时确认（脚本，不经 Agent，不耗 token）
监听器检测到新消息后 **立即** 发微信：
`收到[用户原话]`（中括号内完全复制）

## 阶段 2 — 回答问题（Agent，耗 token）
Agent **只输出答案正文**，不要再写 `收到[...]`。
监听器把 Agent 答案作为 **第二条微信消息** 发给用户。

## 在 Cursor 里手动处理微信时
同样遵循：先 `收到[原话]`，再发答案（可分两条，或用户已收到 ACK 则只发答案）。
