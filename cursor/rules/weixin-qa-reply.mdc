---
description: 微信消息必须回答问题并发回微信
alwaysApply: true
---

# 微信问答通道

当消息来自微信（weixin-mcp / 监听器 / 用户说「查看微信并回复」）时：

1. **微信是用户的提问入口**：用户在微信里问的问题，必须在微信里收到完整回答。
2. **回复必须发回微信**：处理完成后用 `weixin_send` 或 `npx weixin-mcp send` 发送，不能只留在 Cursor 对话里。
3. **回复格式**：
   - 第一行：`收到[用户原话]`（中括号内完全复制）
   - 之后：针对问题的实质回答（清晰、完整、用户可直接阅读）
4. **不要**只确认收到而不回答问题；**不要**让用户来 Cursor 看答案。
