---
description: 三台机器 Cursor 统一配置约定（三机对等）
alwaysApply: true
---

# 多机 Cursor 约定

## 机器与仓库

- **macOS × 1 + Windows × 2**，三台机器**对等**，无主机/从机之分
- 个人配置源仓库：`~/cursor-dotfiles`（Windows: `%USERPROFILE%\cursor-dotfiles`）
- 三台均可 **pull / push Gitee**（`https://gitee.com/gsg733jwb/cursor-dotfiles`）
- 完整环境说明见 `~/cursor-dotfiles/AGENTS.md`（可用 `@AGENTS.md` 引用）

## 同步口令

| 用户说 | macOS | Windows |
|--------|-------|---------|
| **开工**、拉代码、同步配置 | `cd ~/cursor-dotfiles && ./scripts/cursor-sync.sh pull` | `powershell -File "$env:USERPROFILE\cursor-dotfiles\scripts\cursor-sync.ps1" pull` |
| **收工**、更新 Gitee、上传配置 | `./scripts/cursor-sync.sh push` | 同上 `push` |

- 收到口令后 **直接执行**，不要只告诉用户怎么做

## 自动上传（有变更即上传）

三台机器均可启用；改 `rules` / `skills` / `mcp.json` / `hooks` / 编辑器设置后，**约 45 秒防抖**自动 push 到 Gitee。

| 机制 | macOS | Windows |
|------|-------|---------|
| 后台轮询 | LaunchAgent `cursor-sync-watch`（开机自启，每 10s 检测） | `cursor-sync-watch.ps1`（开机自启） |
| Cursor Hook | 编辑配置 / 对话结束 → 防抖上传 | 同上（PowerShell） |
| 日志 | `~/.cursor/sync-auto.log` | `%USERPROFILE%\.cursor\sync-auto.log` |
| 安装 | `scripts/install-mac-sync.sh` | `scripts/install-windows-sync.ps1` |

**开工 pull** 期间会短暂抑制自动上传，避免拉取后误 push。

## 禁止事项

- **不要**修改 `~/.cursor/skills-cursor/`（Cursor 内置，各机自动同步）
- **不要**把项目规范写入 dotfiles；项目规则放各仓库 `.cursor/rules/`
- **不要**在 Git 中提交明文 API Key、token、`.env`；MCP 密钥用环境变量
- **Windows 禁止**在 `hooks.json` 里注册 `.sh` 脚本（会启动 Git Bash 卡住）

## Agent 上下文

- 聊天历史**不能**跨机；跨机延续任务靠 `AGENTS.md`、Rules、`@` 引用文件
- 用户说「按多机约定继续」时，先读 `AGENTS.md` 再行动
- 默认**不要**访问 GitHub（国内易超时）；同步目标为 Gitee
