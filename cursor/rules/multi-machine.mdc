---
description: 三台机器当作一台 Cursor（Gitee 对等同步）
alwaysApply: true
---

# 一台 Cursor，三台机器

- **对等**：macOS × 1、Windows × 2，均可对 Gitee pull/push
- 配置仓库：`~/cursor-dotfiles` / `%USERPROFILE%\cursor-dotfiles`
- 跨机共识：读 `AGENTS.md`（`@AGENTS.md`）；聊天历史不跨机
- 配置变更约 **45 秒防抖后自动 push Gitee**；其他机器「开工」pull 即可对齐

## 口令（直接执行，勿只口述）

| 用户说 | Windows |
|--------|---------|
| **开工**、拉代码、同步配置 | `powershell -NoProfile -ExecutionPolicy Bypass -File "$env:USERPROFILE\cursor-dotfiles\scripts\cursor-sync.ps1" pull` |
| **收工**、更新gitee、上传配置 | 同上路径，`push` |

## 禁止

- 默认 **不要** `git fetch/pull origin`、不要访问 GitHub（除非用户明确要求）
- 不要改 `skills-cursor/`；不要提交明文密钥
- Windows hooks.json **不要**注册 `.sh` 脚本
